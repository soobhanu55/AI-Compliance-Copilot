// Single place to point the frontend at a backend. For local development this is already
// correct. For a deployed frontend (e.g. GitHub Pages), change this to your deployed backend's
// URL (e.g. your Hugging Face Space) — see ../../DEPLOYMENT.md.
window.STATUTA_API_BASE = "http://localhost:8000";
